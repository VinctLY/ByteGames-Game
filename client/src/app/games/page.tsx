import { minecraft } from "../layout";

export default function page() {
	return <main className={`${minecraft.className} min-h-screen p-8 flex justify-center items-center`}>No games for now..</main>;
}
